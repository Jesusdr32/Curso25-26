package multiprocesoIII;

import java.io.File;
import java.io.IOException;
import java.util.Scanner;

public class Asignaturas_Fichero {

	public static void main(String[] args) throws IOException, InterruptedException {
		Scanner sc = new Scanner(System.in);
		
		File directorio = new File("C:\\Users\\Vespertino\\Desktop\\DA2D1E\\PSP+PMDM\\Unidad2\\ProcessBuilder\\Hijos\\bin");
		
		ProcessBuilder pb = new ProcessBuilder("java", "paquete.MediasAsignaturas");
		
		pb.directory(directorio);
		
		Process p = pb.start();
		
		BufferedReader br = new BufferedReader(new OutputStreamReader(p.getOutputStream()));
		
		BufferedWriter bw = new BufferedWriter(new InputStreamWriter(p.getInputStream()));
		
		String asignatura = "";
		
		String nombreFichero = "";
		
		do {
		
		System.out.println("Introduce el nombre de la asignatura: ");
		
		asignatura = sc.nextLine();
		
		System.out.println("Introduce el nombre del fichero: ");
		
		nombreFichero = sc.nextLine();
		
		} while (asignatura.contains("*") && nombreFichero.contains("*"));
	}

}
